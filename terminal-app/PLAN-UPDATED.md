# PLAN-UPDATED.md — superseded

This file is no longer maintained. Its phase-status table has been folded into
`PLAN.md` → **"Phases checklist"**, which is now the single source of truth for
phase status. The "outstanding" items it once listed (server deploy, `git push`,
hardware smoke tests) are all done.

See [`PLAN.md`](PLAN.md).
